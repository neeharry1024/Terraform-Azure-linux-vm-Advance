#cloud-config

package_update: true
package_upgrade: true

packages:
  - nginx

runcmd:
  - systemctl enable nginx
  - systemctl start nginx
  - echo "<h1>Production VM Deployed with Terraform</h1>" > /var/www/html/index.html
